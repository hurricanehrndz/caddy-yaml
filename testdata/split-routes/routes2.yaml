apps:
  http:
    servers:
      yhap:
        routes:
          - match:
              - host: [api.example.com]
            handle:
              - handler: reverse_proxy
                upstreams:
                  - dial: localhost:8080
