apps:
  http:
    servers:
      srv0:
        listen: [":443"]
        routes:
          - handle:
              - handler: static_response
                status_code: "200"
                body: "{env.HOST}"
