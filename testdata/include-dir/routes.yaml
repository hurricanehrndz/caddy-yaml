x-static_response_body: &static_response_body "Hello from directory include"

apps:
  http:
    servers:
      srv0:
        routes:
          - match:
              - host: [example.com]
            handle:
              - handler: static_response
                body: *static_response_body
